# Diganti oleh deploy-github-pages.yml (portfolio + admin)
name: Deploy Admin (deprecated)

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy sekarang via .github/workflows/deploy-github-pages.yml"